{
 "cpp12_01": {
   "ch": "C++ 제12장 · 템플릿·STL", "sec": "12.1 반복자(iterator)",
   "title": "iterator — 순회의 공통 언어",
   "narr": "vector·list·map은 속이 완전히 다른데 어떻게 같은 for 문으로 훑을까요? 답은 반복자입니다 — \"지금 이 위치\"를 가리키는 포인터 같은 손가락이죠. begin()이 첫 칸, end()가 마지막 바로 다음입니다.",
   "more": "for(auto it=v.begin(); it!=v.end(); ++it) — begin/end/++/* 인터페이스는 컨테이너가 달라도 같습니다. *it로 현재 값을 보고, 이 공통 인터페이스 덕에 sort·find가 어떤 컨테이너에도 통합니다.",
   "more_en": "An iterator is a pointer-like handle to a position. begin() is the first, end() is one-past-last; ++it advances, *it dereferences. This uniform interface lets algorithms work on any container.",
   "problem": "end()는 마지막 원소를 가리킬까요, 마지막 다음(빈 위치)을 가리킬까요? (정답: 마지막 다음 — 그래서 it != v.end() 로 반복을 끝냅니다)"
 },
 "cpp12_02": {
   "ch": "C++ 제12장 · 템플릿·STL", "sec": "12.2 sort · find",
   "title": "sort · find — 정렬과 탐색, 한 줄로",
   "narr": "직접 정렬을 짤 필요 없이 sort(v.begin(),v.end()) 한 줄이면 O(n log n) 최적 정렬을 돌립니다. find(begin,end,x)는 값을 앞에서부터 훑어 그 위치의 반복자를 돌려주고, 없으면 end()를 줍니다.",
   "more": "sort(v.begin(),v.end())는 반복자 한 쌍만으로 O(n log n) 정렬(보통 introsort). find(begin,end,8)은 위치의 반복자를, 못 찾으면 end()를 반환합니다. 정렬본이면 binary_search로 O(log n).",
   "more_en": "sort takes a pair of iterators and runs O(n log n) sorting (typically introsort). find returns the iterator to the value or end() if absent. On sorted data, binary_search gives O(log n).",
   "problem": "find가 값을 찾지 못하면 무엇을 반환할까요? (정답: end() — 그래서 반환값을 v.end()와 비교해 존재 여부를 판정합니다)"
 },
 "cpp12_03": {
   "ch": "C++ 제12장 · 템플릿·STL", "sec": "12.3 accumulate · for_each · transform",
   "title": "accumulate · for_each · transform",
   "narr": "루프를 손으로 짜는 대신 \"무엇을 할지\"만 알고리즘에 넘깁니다. accumulate는 원소를 하나로 접고(합계), for_each는 각 원소마다 동작을 실행하고, transform은 원소마다 변환식을 적용해 새 배열을 만듭니다.",
   "more": "accumulate(v.begin(),v.end(),0)은 초깃값 0에서 시작해 다 더한 합계. for_each는 각 원소에 부수효과를, transform은 x→x*2처럼 원소별 변환으로 같은 길이의 새 배열을 만듭니다(함수형 reduce/map).",
   "more_en": "accumulate folds elements into one value (sum from init 0). for_each applies an action per element; transform maps each element to a new one (x->x*2). These are the reduce/map of functional style.",
   "problem": "accumulate와 transform의 결과 길이는 어떻게 다를까요? (정답: accumulate는 접어서 하나의 값, transform은 원소마다 변환해 원본과 같은 길이의 배열)"
 },
 "cpp12_04": {
   "ch": "C++ 제12장 · 템플릿·STL", "sec": "12.4 람다식",
   "title": "람다식 — 그 자리에서 만드는 함수",
   "narr": "정렬 기준이나 필터 조건은 대개 한 줄짜리라 이름 붙은 함수를 따로 두긴 번거롭죠. 람다는 그 자리에서 즉석으로 함수를 만듭니다: [캡처](매개변수){ 본문 }.",
   "more": "[](int a,int b){return a>b;}를 sort의 셋째 인자로 주면 내림차순 기준이 됩니다. [th](int x){return x>th;}처럼 대괄호에 바깥 변수를 담으면(캡처) 본문에서 그 값을 쓸 수 있습니다.",
   "more_en": "A lambda is an inline anonymous function: [capture](params){body}. Passed as sort's third arg it becomes the ordering; the capture [th] lets the body use an outer variable — the key difference from a plain function pointer.",
   "problem": "람다 [th](int x){return x>th;} 에서 대괄호 안의 th는 무엇을 뜻할까요? (정답: 캡처 — 바깥 변수 th를 람다 본문에서 쓰도록 잡아 오는 것)"
 },
 "cpp12_05": {
   "ch": "C++ 제12장 · 템플릿·STL", "sec": "12.5 람다 + 알고리즘",
   "title": "람다 + 알고리즘 — 진짜 힘은 조합",
   "narr": "람다와 STL 알고리즘이 만나면 루프·인덱스·임시변수 없이 \"무엇을\"만 말하는 선언적 처리가 됩니다. 같은 알고리즘에 람다만 갈아 끼우면 정렬 기준·조건이 통째로 바뀝니다.",
   "more": "sort(begin,end,[](a,b){return a.second>b.second;})는 pair의 점수로 내림차순, count_if(begin,end,[](p){return p.second>=90;})는 조건 만족 개수를 셉니다. 같은 조건 람다를 find_if·copy_if·partition에 재사용할 수 있습니다.",
   "more_en": "Lambdas + STL algorithms give declarative data processing. Swap the lambda to change the ordering or predicate; count_if counts matches, and the same predicate reuses in find_if/copy_if/partition. This is modern STL style.",
   "problem": "점수 pair 벡터에서 90점 이상인 사람 수를 구하려면 어떤 알고리즘과 람다를 쓸까요? (정답: count_if(begin,end,[](p){return p.second>=90;}) — 조건을 만족하는 원소 개수를 셈)"
 }
}

# AGENTS.md — 에이전트·작업자 공통 규칙 (EduViz)

> 이 파일은 **콘텐츠를 쓰는 모든 주체**(메인 세션·하위 에이전트·스킬)가 지켜야 하는 규칙을 담습니다.
> 아키텍처·작업 절차는 `CLAUDE.md`, 지금 손의 위치는 `SESSION_HANDOFF.md` 를 보세요.

## 1. 학습자료 화법 — 제작 흔적 금지 (2026-08-29 사령관 지시 · 최우선 · 예외 없음)

**독자는 오직 이 자료로 공부하려는 학습자 한 사람뿐입니다.** 제작자도, 검수자도, 지시를 내린 사람도 독자가 아닙니다.
화면에 나가는 모든 글(HTML 본문·script 안 문자열·JSON 텍스트·캡션·팝업·툴팁)에서 **자료가 어떻게 만들어졌는지에 관한 말**을 전부 없앱니다.

### 금지 5유형
1. **구현·제작 자랑** — "이 페이지가 그 자리에서 실제로 계산한 값입니다", "미리 적어 둔 숫자가 없습니다", "하드코딩하지 않았습니다", "라이브러리 없이 정의대로 적분해서 그립니다", "엔진이 매 프레임 적분합니다", "코드로 검증했습니다". 골든룰(가짜 금지)은 **지켜야 할 제작 기준이지 학습 내용이 아닙니다.** 지켰다는 사실을 화면에 쓰지 않습니다.
2. **수정 이력·자기 교정** — "앞서 잘못 쓴", "바로잡았습니다", "이전에는 ~라고 했지만", "보완했습니다", "다시 썼습니다".
3. **대화 흔적** — "말씀하신 대로", "지적하신", "요청하신", "질문 주신", 사령관·사용자 지칭. **지시를 받아 고친 답변 문장을 그대로 자료에 붙여넣지 않습니다.**
4. **집필 계획 노출** — "이 절은 나중에 채웁니다", "아직 준비 중", "다음 버전에서", "TODO", 제작 진행률.
5. **자료 밖 참조** — 이 자료에 없는 다른 장면·실험실 내용을 여기 있는 것처럼 말하기, 존재하지 않는 절·장면 번호.

### 허용 (지우면 안 되는 것)
- 학습 동선 안내: "지금은 이렇게만 해 두고 §4에서 다룹니다", "다음 실험실에서 이어집니다".
- 자료 출처 안내: "아래 수치는 위 32개 자료에서 나온 것입니다"(사실 안내이지 구현 이야기가 아님).
- 조작 안내: "슬라이더를 움직여 보세요", "D 키로 한 단계씩".
- 튜터에게 질문을 권하는 블록.

### 바꾸는 법
지우고 끝내지 말고, 그 자리에 **학습자가 알아야 할 내용**을 넣습니다. 예)
- 나쁨: "여기는 미리 계산한 값이 없습니다. 전부 실시간 계산입니다."
- 좋음: (삭제) 또는 "아래 값은 모두 위 표의 32개 숫자에서 나온 것입니다."
문장을 지워 맥락이 끊기면 연결 문장을 한 줄 넣습니다.

### 자가 점검 (콘텐츠 커밋 전 필수)
**`node tools/voice_check.mjs`** — `adp/` · `content/` · `js/` 를 훑어 위 5유형 문구를 찾아 준다(코드 주석은 건너뛴다). 히트가 나오면 학습자 화면에 나가는 문장인지 눈으로 갈라서 처리한다(뜻이 다른 정상 문장이 걸리는 경우가 있다 — 예: "중력을 시공간의 굴곡으로 다시 썼습니다").
직접 훑고 싶을 때의 grep:
```
grep -rnE '하드코딩|실제로 계산한 값|미리 (적어|계산)|라이브러리 없이|실시간으로 계산|매 프레임|코드로 검증|바로잡|보완했|다시 썼|이전 버전|앞서 잘못|말씀|지적하신|요청하신|사령관|TODO|준비 중|나중에 채' \
  adp/ content/ js/content_*.js
```
히트가 있으면 학습 문장인지 코드 주석인지 눈으로 갈라서 처리합니다(코드 주석은 남겨도 됩니다 — 화면에 안 나오므로).
물리 트랙에는 같은 취지의 선행 규칙이 `CLAUDE.md` §1-7(파인만 화법)에 있습니다. 이 규칙은 **전 트랙·전 자료**로 확장한 것입니다.

## 2. 그 밖의 상시 규칙 (요약 — 상세는 CLAUDE.md)
- 존댓말 · LaTeX 금지(유니코드 평문 표기) · 흔치 않은 한자어는 영어 병기.
- 골든룰: 화면의 모든 수치는 실계산값. (지켰다는 말은 화면에 쓰지 않음 — 위 §1-1)
- ADP 페이지 수정 후 커밋 전 `node tools/ui_check.mjs` 전부 ✅.
- 콘텐츠 추가·변경 시 `content/search_index.json` 재생성, `adp/sw.js` 캐시 버전 올리기.

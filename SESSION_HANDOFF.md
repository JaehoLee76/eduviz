# SESSION_HANDOFF — 지금 손이 어디에 있는가

> "대화 이어서 하자" 를 들으면 **이 파일부터** 읽고 곧바로 재개합니다(되묻지 않음).
> 규칙: 전역 `~/.claude/CLAUDE.md` §세션 이어가기 규약 · 프로젝트 `CLAUDE.md` §10.
> 이 파일은 **한 장짜리 현재 상태**입니다. 누적 로그가 아니라 매번 덮어씁니다.

## 언제
2026-08-22

## 무엇을 하던 중
**개인 학습 흐름**(콘텐츠 제작 아님). ADP Master 트랙에서 **자유도 (degrees of freedom)** 개념을 공부하는 중이었습니다.

## 정확한 위치
- 파일: `adp/topics/degrees-of-freedom.js` (개념 사전 항목, 배포 완료 — 커밋 `732cafa`)
- 그 글이 이미 담고 있는 것: ①자유도의 정의(평균 조건이 걸리면 마지막 하나는 자동 결정) ②표본분산을 n−1로 나누는 이유(30만 번 실측: n=3.202 vs n−1=4.002) ③분산분석의 31 = 3 + 28 분해 ④자유도별 t 임계값 2.920 → 1.645

## 다음 한 걸음
아직 다루지 않은 세 대목 중 사령관이 고르는 것부터 설명합니다.
1. **카이제곱 독립성 검정의 (r−1)(c−1)** — 왜 뺄셈이 아니라 곱셈인가
2. **회귀의 n − k − 1** — 수정 R²가 변수 추가에 벌점을 매기는 원리
3. **자유도 = 차원(기하적 그림)** — 편차 벡터가 '합이 0인 평면'에 갇혀 n−1차원만 움직인다. 이게 잡히면 위 규칙들이 한 원리로 합쳐짐

## 열린 질문
- 위 세 갈래 중 어디부터 갈지 미정
- 설명 후 그 내용을 `degrees-of-freedom.js` 에 덧붙여 개념 사전을 키울지, 별도 주제 파일로 뗄지 미정

## 대기 중인 제작 큐 (학습 흐름과 별개, 지시 있을 때만)
- 통계 실험실 ⑥베이즈 ⑦이원배치 ⑧비모수 ⑨편상관 ⑩생존·분위수회귀 ⑪선형계획법
- 머신러닝 파트 6단계(EDA·전처리가 최대 병목)
- 방식: 4개씩 하위 에이전트 병렬 → 파이썬 정답 대조 → `node tools/ui_check.mjs` 전부 ✅ → 허브·서비스워커 등록 → 배포

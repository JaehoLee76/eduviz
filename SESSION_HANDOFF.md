# SESSION_HANDOFF — 지금 손이 어디에 있는가

> "대화 이어서 하자" 를 들으면 **이 파일부터** 읽고 곧바로 재개합니다(되묻지 않음).
> 규칙: 전역 `~/.claude/CLAUDE.md` §세션 이어가기 규약 · 프로젝트 `CLAUDE.md` §10.
> 한 장짜리 현재 상태입니다. 누적하지 말고 덮어씁니다.

## 언제
2026-08-29

## 무엇을 하던 중
**개인 학습 흐름**(ADP Master) 중, 사령관이 `adp/labs/0008-distribution-family.html`(자유도와 분포의 계보)을 읽다가 **제작 과정 말투가 그대로 노출된 문제**를 지적 → 규칙 등록 + 전 자료 일괄 정리를 마쳤습니다. 학습 자체(§2 이후 읽기)는 아직 재개 전입니다.

## 정확한 위치
- **규칙 등록**: 루트 **`AGENTS.md` 신설** §1 "학습자료 화법 — 제작 흔적 금지"(금지 5유형·허용 목록·고치는 법·자가점검). `CLAUDE.md` §1 절대규칙에 **8번**으로 같은 규칙 추가(상세는 AGENTS.md 가리킴).
- **검사기 신설**: `node tools/voice_check.mjs` — adp/·content/·js/ 에서 제작 흔적 문구를 훑음(코드 주석 제외). 현재 남은 2건은 뜻이 다른 정상 문장(phys0 "중력을 시공간의 굴곡으로 다시 썼습니다", cpp10 "타입을 나중에 채우는").
- **일괄 정리 완료**: 0008 7건 · 0005~0007 9건 · 0002~0004 9건 · `content/calc0·1·3.json` 3건("공식을 베낀 게 아니라" 류) · `adp/index.html` 로드맵 문구 1건. 실험실 풀이 시작 배너 문구도 하나로 통일("여기서부터 위 데이터를 이용한 풀이입니다 (1→5 순서 …)"). `adp/topics/*` 는 해당 없음(몬테카를로 "직접 만들어 확인했습니다"는 교습 화법이라 유지).
- 검증: `node tools/ui_check.mjs` PC·모바일 전부 ✅ · 인라인 스크립트 전수 `node --check` ✅ · `adp/sw.js` 캐시 v18.

## 다음 한 걸음
1. **학습 재개** — 사령관이 0008 §2(계보 지도)부터 읽고 이상한 대목 지목 → 그 자리에서 개편.
2. 그다음 **0002 본편 정복** — 전제검정(정규성·등분산)·사후검정(본페로니·Tukey).
3. 아직 안 다룬 자유도 갈래: 회귀의 n−k−1, 웰치 t 의 소수 자유도.

## 열린 질문
- 0008 의 §1 설명을 `adp/topics/` 주제 파일로 떼어 다른 실험실에서도 재사용할지

## 대기 중인 제작 큐 (학습 흐름과 별개, 지시 있을 때만)
- 통계 실험실 6)베이즈 7)이원배치 8)비모수 9)편상관 10)생존·분위수회귀 11)선형계획법
- 머신러닝 파트 6단계(EDA·전처리가 최대 병목)

# 알고리즘 진행 순서 (SSOT) — 수학 트랙과 동일 morph 방식

> 수학(math.html)과 같은 엔진(engine.js)·5원칙(예고↔회수)·한영병기·화면번호·학습패널을 그대로 사용.
> ★강점: **수학 트랙을 적극 회수** — 가우스 합(13장)→O(1), 로그(7장)→O(log n), 함수 성장(5·7장)→복잡도 곡선,
> 피보나치(13장 수열)→DP, 그래프(6장 좌표)→네트워크.
> 진행축: 알고리즘·복잡도 → 자료구조 → 정렬 → 탐색 → 트리 → 그래프 → 동적계획법 → 설계 패러다임.

## 제1장 알고리즘과 복잡도
- 1.1 알고리즘이란 (명확한 단계·입출력·유한성)
- 1.2 효율성 (같은 답, 다른 비용)
- 1.3 빅오 표기법 (O(1)·O(log n)·O(n)·O(n²)·O(2ⁿ))

## 제2장 자료구조
- 2.1 배열 (인덱스 접근 O(1))
- 2.2 연결 리스트 (포인터)
- 2.3 스택과 큐 (LIFO·FIFO)
- 2.4 해시 테이블 (O(1) 조회)

## 제3장 정렬
- 3.1 기초 정렬 (버블·선택·삽입, O(n²))
- 3.2 병합 정렬 (분할정복, O(n log n))
- 3.3 퀵 정렬 (피벗 분할)

## 제4장 탐색
- 4.1 선형 탐색 (O(n))
- 4.2 이분 탐색 (O(log n), 정렬 전제)

## 제5장 트리
- 5.1 이진 트리·순회
- 5.2 이진 탐색 트리 (BST)
- 5.3 힙과 우선순위 큐

## 제6장 그래프
- 6.1 그래프 표현 (인접 행렬·리스트)
- 6.2 너비 우선(BFS)·깊이 우선(DFS)
- 6.3 최단 경로 (다익스트라)

## 제7장 동적 계획법
- 7.1 메모이제이션 (피보나치)
- 7.2 최적 부분 구조 (배낭·LCS)

## 제8장 알고리즘 설계 패러다임
- 8.1 그리디
- 8.2 분할 정복
- 8.3 계산의 한계 (P vs NP) + 종합 피날레

---

## 빌드 현황
- ✅ **제1장 알고리즘과 복잡도** (#1~#5): 알고리즘이란 · 효율성(O(n)vs O(1)) · 빅오 곡선 · O(log n) · 복잡도 등급표
- ✅ **제2장 자료구조** (#6~#10): 배열 · 연결리스트 · 스택 · 큐 · 해시테이블
- ✅ **제3장 정렬** (#11~#15): 버블 · 선택 · 삽입 · 병합 · 퀵 (모두 tap step 애니)
- ✅ **제4장 탐색** (#16~#19): 선형 · 이분 · 선형vs이분 · 탐색전략
- ✅ **제5장 트리** (#20~#24): 이진트리 · 순회 · BST · 힙 · 균형
- ✅ **제6장 그래프** (#25~#29): 그래프란 · 표현(리스트/행렬) · BFS · DFS · 다익스트라
- ✅ **제7장 동적 계획법** (#30~#34): 피보나치 재귀폭발 · 메모이제이션 · 타뷸레이션 · DP 두조건 · 격자DP(파스칼)
- ✅ **제8장 설계 패러다임** (#35~#39): 그리디 · 그리디함정 · 분할정복 · P vs NP · 피날레
> algo.html = 파란 테마, engine.js + js/algo_helpers.js(AV: arr/node/arrow/pointer/bars) 공유, 캐릭터=파란 네모. 홈 버튼 활성화. content_algo5.js drawTree(완전이진트리 배열인덱싱)·content_algo6.js drawGraph(정점위치+인접리스트)·content_algo7.js drawFT(fib 호출트리) 로컬 헬퍼.

## ★★★ 알고리즘 트랙 — 뼈대 8장 + 표준 고급 알고리즘 분기 전면 확장
**뼈대 39장면(#1~#39)** + **분기(세부) 169종(175 배치, 6종 교차배치)** = 약 214장면. 전 분기 goTo 스캔 렌더 확인, 콘솔 에러 0.

### 계층형 뼈대-분기 구조 (사령관 설계, content_algo_br.js + algo_br.json)
- 뼈대(spine)=기본 진행축, 세부=분기(branch, scene.branchOf='<뼈대id>'). 분기 들어갔다(↓/🔍) 나오기(↑/↩).
- breadcrumb 위치표시 + #계층번호(#15.1) + TOC 하위요소. 각 분기=concept 캔버스 애니 + 한영병기 more/문제.
- 교차배치 6종(세그트리·펜윅·LCA·트라이·소수체·확장유클리드)=두 관련 챕터에서 모두 접근.

### 분기 169종 — 표준 CS·경쟁 프로그래밍 알고리즘 망라
- **정렬·선택**: 정렬하한(결정트리)·계수·기수·순서통계(quickselect)·힙정렬·build-heap O(n)·퀵 최악/랜덤화
- **자료구조**: 레드블랙·AVL·B-트리·트립·스플레이·union-find·롤백DSU(오프라인동적연결성)·세그트리·펜윅(BIT)·2D펜윅·머지소트트리·희소테이블·평방분할·Mo's·영속세그·세그트리빔즈·리차오·스킵리스트·피보나치힙·쌍힙·링크컷트리·van Emde Boas·웨이블릿트리·좌표압축
- **트리**: 이진트리순회·BST삽입/삭제·LCA·타잔오프라인LCA·HLD·트리DP·센트로이드분할·오일러투어·가상트리·DFS간선분류·단절점/다리·BCC·도미네이터트리
- **그래프**: 위상정렬·SCC·2-SAT·BFS/0-1BFS·벨만포드·플로이드·존슨·DAG최단/최장·다익스트라증명·A*·MST·오일러경로·Hierholzer·이분판정·이분매칭·호프크로프트카프·블로섬·헝가리안·안정결혼·최대유량·디닉·MCMF·하한유량·슈토어바그너·카거·Gomory-Hu·최소평균사이클·키르히호프(신장트리수)
- **문자열**: KMP·Z함수·매내처·아호코라식·접미사배열·접미사오토마톤·우코넨접미사트리·트라이·문자열해싱
- **DP**: 막대자르기·LCS·LIS·편집거리·0/1배낭·행렬연쇄·카데인·비트마스크·자릿수·트리DP·SOS·CHT·분할정복최적화·크누스최적화·Aliens트릭(WQS)·SMAWK·정수분할·스타이너트리(부분집합DP)
- **정수론·조합**: 에라토스테네스체·오일러피·확장유클리드·CRT·밀러라빈·폴라드로·뫼비우스·이산로그(BSGS)·빠른거듭제곱·RSA·토넬리샹크스·뤼카정리·포함배제·카탈란수·번사이드·MITM
- **기하**: 볼록껍질·회전캘리퍼스·가장가까운점쌍·선분교차(CCW)·삼분탐색
- **수치·수학**: 가우스소거·LU분해·FFT·NTT·카라츠바·행렬거듭제곱·스트라센·라그랑주보간·뉴턴법·벨캄프매시·경사하강·k평균·선형계획
- **그리디·설계·랜덤**: 허프만·활동선택·동전교환·분할가능배낭·근사·환원·온라인·병렬·분할상환·마스터정리·시뮬레이티드어닐링·DLX(댄싱링크)·님과그런디·비트셋가속·XOR선형기저·분수계획법(Dinkelbach)
- **이분·기타**: lower/upper_bound·이분3대함정·Stern-Brocot·슬라이딩윈도우·투포인터·단조스택·슬라이딩최댓값·저수지샘플링·보이어무어다수결·비트연산트릭·루프불변식

★수학 트랙 회수: 가우스합→O(1)·정렬하한, 로그→이분/희소테이블, 파스칼→격자DP·카탈란, 피보나치·무한급수→DP·행렬거듭제곱·벨캄프매시, 6장 그래프.

### 잔여 후보(초전문·연구급 주제, 미구현)
심플렉스 상세·suffix automaton 응용·웨이블릿 행렬·평면성 판정·Steiner forest·일반 매칭 가중판·persistent treap — 대부분 기구현 기법의 변형/응용. 표준+경쟁 PS 정전(canon)은 사실상 망라.

_규모: 뼈대 8장 39 + 분기 169종(175 배치) ≈ 214장면. 검증: node --check + json.load + goTo 인덱스 스캔(렌더+콘솔에러 0)._
_마지막 갱신: 2026-06-17 (표준+경쟁 고급 알고리즘 분기 23→169종 전면 확장, 배치 12~26)_
